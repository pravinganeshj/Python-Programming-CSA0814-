code:
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

rows = len(matrix)
cols = len(matrix[0])
if rows != cols:
    print("The matrix is not square. Diagonal elements are not defined.")
else:
    diagonal_sum = 0

    print("Diagonal elements:")
    for i in range(rows):
        print(matrix[i][i])
        diagonal_sum += matrix[i][i]

    print(f"\nSum of diagonal elements: {diagonal_sum}")


output:
diagonal elements:1
5
9
sum: 15
